# CMakeList.txt : CMake project for llp_lab1, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

# Add source to this project's executable.
add_executable (llp_lab1 "main.c" "utils/input.h" "utils/input.c" "zgdb/format.c" "zgdb/format.h" "zgdb/index.h" "zgdb/index.c" "zgdb/block.h" "zgdb/block.c")

if (CMAKE_VERSION VERSION_GREATER 3.12)
  set_property(TARGET llp_lab1 PROPERTY C_STANDARD 17)
endif()

# TODO: Add tests and install targets if needed.
